import styled from 'styled-components';

export default styled.button`
  font: inherit;
  cursor: pointer;
  width: auto;
  height: auto;
  padding: 0;
  border: none;
  outline: none;
  background-color: transparent;
`;
